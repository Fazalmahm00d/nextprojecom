import CartMain from "./CartMain";

export const metadata={
    title:"Your Cart",
    description:"cart page"
}

const About=()=>{
    return(
        <div>
            <CartMain/>
        </div>
    )
}

export default About;